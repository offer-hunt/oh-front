# OIDC/OAuth2 Configuration
# ---
# URL of your OIDC provider (e.g., Keycloak, Auth0)
VITE_OIDC_AUTHORITY=https://example-auth.com/realms/my-realm
# Your client ID
VITE_OIDC_CLIENT_ID=my-frontend-client
# The callback URL where the auth provider will redirect after login.
# For local development with docker-compose, this should be http://localhost:5173/auth/callback
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
# OIDC scopes you are requesting
VITE_OIDC_SCOPE="openid profile email"

# API Configuration
# ---
# Base URL for the frontend to make API calls to during local development (npm run dev)
VITE_BACKEND_API=/api

# This variable is used by Nginx inside the Docker container to proxy requests.
# `host.docker.internal` is a special DNS name that resolves to the host machine's IP,
# useful when the backend is running on your local machine outside of Docker.
# If your backend is another service in the same docker-compose file, you'd use its service name, e.g., http://backend:8080/api
VITE_BACKEND_API_DOCKER=http://host.docker.internal:8080/api

VITE_AUTH_USE_MOCKS=true
VITE_COURSES_USE_MOCKS=true
VITE_PROFILE_USE_MOCKS=false
VITE_USE_MOCKS=false
VITE_BACKEND_API=/api
